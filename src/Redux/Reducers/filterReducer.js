const initalState = {
    filters:{
        first:false,
        last:false,
    },
};
export const filterReducer = (state = initalState,action)=>{
    switch(action.type){
        default:return state
    }
}